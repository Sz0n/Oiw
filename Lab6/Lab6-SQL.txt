--SELECT-y--
SELECT * FROM `account` WHERE LAST_ACTIVITY_DATE = '2004-12-15'

SELECT `AVAIL_BALANCE` FROM `account`

--AND/OR?NOT--
SELECT `PENDING_BALANCE`, `AVAIL_BALANCE`,`OPEN_DATE`, `LAST_ACTIVITY_DATE`
FROM account WHERE `OPEN_DATE` = '2003-07-30' AND `LAST_ACTIVITY_DATE` = '2004-12-15'

SELECT `ACCOUNT_ID`, `AVAIL_BALANCE`, `LAST_ACTIVITY_DATE`,`OPEN_DATE`,`PENDING_BALANCE`
FROM account WHERE NOT `OPEN_DATE` = '2003-07-30';

--ORDER BY--
SELECT * FROM account ORDER BY AVAIL_BALANCE ASC

--INSERT INTO--
INSERT INTO `account`(`ACCOUNT_ID`, `AVAIL_BALANCE`, `CLOSE_DATE`, `LAST_ACTIVITY_DATE`, `OPEN_DATE`, `PENDING_BALANCE`, `STATUS`, `CUST_ID`, `OPEN_BRANCH_ID`, `OPEN_EMP_ID`, `PRODUCT_CD`) 
VALUES (111, 11111, NULL, 2018-11-27, 2018-11-26, 22222, 'ACTIVE', 1, 1, 1, 'CD')

--NULL--
SELECT * FROM `account` WHERE `CLOSE_DATE` IS NULL
SELECT * FROM `account` WHERE `CLOSE_DATE` IS NOT NULL

--UPDATE--
UPDATE `account` 
SET `ACCOUNT_ID`=1234,`AVAIL_BALANCE`=1234,`CLOSE_DATE`= NULL,`LAST_ACTIVITY_DATE`=NULL,`OPEN_DATE`=NULL,`PENDING_BALANCE`=1234,`STATUS`='ACTIVE'
WHERE `ACCOUNT_ID` = 111

--DELETE--
DELETE FROM `account` WHERE `ACCOUNT_ID` = 1234

--SELECT TOP--
SELECT * FROM `account` WHERE `AVAIL_BALANCE` > 1000 LIMIT 5

--MIN&MAX--
SELECT MIN(`LAST_ACTIVITY_DATE`) FROM `account`
SELECT MAX(`LAST_ACTIVITY_DATE`) FROM `account`

--AVG&SUM--
SELECT SUM(`AVAIL_BALANCE`) FROM `account` WHERE `PENDING_BALANCE` > 1000
SELECT AVG(`AVAIL_BALANCE`) FROM `account` WHERE `PENDING_BALANCE` < 1000

--LIKE--
SELECT ACCOUNT_ID, OPEN_DATE, AVAIL_BALANCE FROM `account` WHERE `OPEN_DATE` LIKE '2004-%'
SELECT ACCOUNT_ID, OPEN_DATE, AVAIL_BALANCE FROM `account` WHERE `OPEN_DATE` LIKE '%-12'

--IN--
SELECT ACCOUNT_ID, OPEN_DATE, AVAIL_BALANCE, PRODUCT_CD FROM `account` WHERE `PRODUCT_CD` IN ('CD', 'BUS')

--BETWEEN--
SELECT ACCOUNT_ID, OPEN_DATE, AVAIL_BALANCE, PRODUCT_CD FROM `account` WHERE `AVAIL_BALANCE` BETWEEN 350 AND 1850

--ALIASES--
SELECT ACCOUNT_ID AS RANDOMOWE_NUMERKI FROM account ORDER BY ACCOUNT_ID ASC

--INNER JOIN--
SELECT account.ACCOUNT_ID, account.PRODUCT_CD, product.NAME 
FROM `account` 
INNER JOIN product WHERE account.PRODUCT_CD=product.PRODUCT_CD ORDER BY ACCOUNT_ID ASC

--UNION--
SELECT First_Name, Last_Name FROM individual 
UNION 
SELECT First_Name, Last_Name FROM employee 
ORDER BY Last_Name